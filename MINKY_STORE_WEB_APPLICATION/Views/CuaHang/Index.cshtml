@model List<EF_CODE_FIRST_FINAL_ASSIGNMENT.DomainClass.CuaHang>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="row mt-3">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-body">
                <div class="card-title">Quản lý cửa hàng</div>
                <hr>
                <form action="/cuahang/create" method="post">
                    <div class="row">
                        <div class="form-group col-6">
                            <label>Mã cửa hàng</label>
                            <input type="text" class="form-control" name="Ma" placeholder="Nhập mã cửa hàng">
                        </div>
                        <div class="form-group col-6">
                            <label>Tên cửa hàng</label>
                            <input type="text" class="form-control" name="Ten" placeholder="Nhập tên cửa hàng">
                        </div>
                        <div class="form-group col-6">
                            <label>Địa chỉ</label>
                            <input type="text" class="form-control" name="DiaChi" placeholder="Nhập địa chỉ">
                        </div>
                        <div class="form-group col-3">
                            <label>Thành phố</label>
                            <input type="text" class="form-control" name="ThanhPho" placeholder="Nhập thành phố">
                        </div>
                        <div class="form-group col-3">
                            <label>Quốc gia</label>
                            <input type="text" class="form-control" name="QuocGia" placeholder="Nhập quốc gia">
                        </div>
                    </div>
                    <div class="form-group">
                        <button class="btn btn-light px-5 mr-2">Thêm cửa hàng</button>
                        <button type="submit" class="btn btn-success px-5">Sửa cửa hàng</button>
                    </div>
                </form>
                <h5 class="card-title">Danh sách cửa hàng</h5>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Mã cửa hàng</th>
                                <th>Tên cửa hàng</th>
                                <th>Địa chỉ</th>
                                <th>Thành phố</th>
                                <th>Quốc gia</th>
                                <th>Thao tác</th>
                            </tr>
                        </thead>
                        <tbody>
                            @for (int i = 0; i < Model.Count; i++)
                            {
                                <tr>
                                    <td>@Model[i].Ma</td>
                                    <td>@Model[i].Ten</td>
                                    <td>@Model[i].DiaChi</td>
                                    <td>@Model[i].ThanhPho</td>
                                    <td>@Model[i].QuocGia</td>
                                    <td>
                                        <a href="/cuahang/detail/@Model[i].Id" class="btn btn-success px-5">Sửa cửa hàng</a>
                                        <a href="/cuahang/remove/@Model[i].Id" class="btn btn-danger px-5" onclick="return confirm('Bạn có chắc muốn xóa?')">Xóa cửa hàng</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

