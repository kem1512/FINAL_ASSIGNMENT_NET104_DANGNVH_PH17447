@model List<MinkyShop.Data.DomainClass.MauSac>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row mt-3">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-body">
                <div class="card-title">Quản lý màu sắc</div>
                <hr>
                <form action="/mausac/create" method="post">
                    <div class="row">
                        <div class="form-group col-6">
                            <label>Mã màu sắc</label>
                            <input type="text" class="form-control" name="Ma" placeholder="Nhập mã màu sắc">
                        </div>
                        <div class="form-group col-6">
                            <label>Tên màu sắc</label>
                            <input type="text" class="form-control" name="Ten" placeholder="Nhập tên màu sắc">
                        </div>
                    </div>
                    <div class="form-group">
                        <button class="btn btn-light px-5 mr-2">Thêm màu sắc</button>
                        <button type="submit" class="btn btn-success px-5">Sửa màu sắc</button>
                    </div>
                </form>
                <h5 class="card-title">Danh sách màu sắc</h5>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th style="width: 20%">STT</th>
                                <th style="width: 25%">Mã màu sắc</th>
                                <th style="width: 25%">Tên màu sắc</th>
                                <th style="width: 30%">Thao tác</th>
                            </tr>
                        </thead>
                        <tbody>
                            @for (int i = 0; i < Model.Count; i++)
                            {
                                <tr>
                                    <th>@i</th>
                                    <td>@Model[i].Ma</td>
                                    <td>@Model[i].Ten</td>
                                    <td>
                                        <a href="/mausac/detail/@Model[i].Id" class="btn btn-success px-5">Sửa màu sắc</a>
                                        <a href="/mausac/remove/@Model[i].Id" class="btn btn-danger px-5" onclick="return confirm('Bạn có chắc muốn xóa?')">Xóa màu sắc</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

