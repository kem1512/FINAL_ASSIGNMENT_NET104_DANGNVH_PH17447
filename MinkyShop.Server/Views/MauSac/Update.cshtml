@model MinkyShop.Data.DomainClass.MauSac
@{
    ViewData["Title"] = "Update";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row mt-3">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-body">
                <div class="card-title">Quản lý màu sắc</div>
                <hr>
                <form action="/mausac/update" method="post">
                    @if (Model != null)
                    {
                        <div class="row">
                            <div class="form-group col-6">
                                <label>Mã màu sắc</label>
                                <input type="text" class="form-control" value="@Model.Ma" name="Ma" placeholder="Nhập mã màu sắc">
                            </div>
                            <div class="form-group col-6">
                                <label>Tên màu sắc</label>
                                <input type="text" class="form-control" value="@Model.Ten" name="Ten" placeholder="Nhập tên màu sắc">
                            </div>
                        </div>
                        <div class="form-group">
                            <button type="submit" value="@Model.Id" name="Id" class="btn btn-success px-5">Sửa màu sắc</button>
                        </div>
                    }
                </form>
            </div>
        </div>
    </div>
</div>

